// Academic Years page - no edit tab needed, years use activate/close/reopen actions instead
document.addEventListener('DOMContentLoaded', function () {
  // Tab switching already handled by app.js
  console.log('Academic Years page loaded');
});
